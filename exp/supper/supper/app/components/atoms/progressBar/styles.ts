import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		width: '100%',
		height: 7,
		borderRadius: 5,
		overflow: 'hidden',
		marginVertical: 10,
	},
	progressBar: {
		height: '100%',
		borderRadius: 5,
	},
});

export default styles;
