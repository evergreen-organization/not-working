import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: { flex: 1 },
	bar: { position: 'absolute', zIndex: 1 },
	top: { marginTop: 0 },
	margin: { marginTop: 10 },
});
