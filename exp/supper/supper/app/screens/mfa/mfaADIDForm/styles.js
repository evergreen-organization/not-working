import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	formContainer: {
		marginVertical: 10,
		marginHorizontal: 20,
	},
	formField: {
		flexDirection: 'row',
		alignItems: 'center',
	},
});
